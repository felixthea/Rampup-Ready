<div class='bulk-new-wrapper'>
  <h2>Add Users</h2>
  <a href="#" class="more-users">More Users</a>
  <form action="<%= bulk_add_users_url %>" method="POST">
  	<input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
  	<%= render 'users/bulk_form' %>
  	<input type="Submit" value="Add Users" id="submit-button">
  </form>
</div>
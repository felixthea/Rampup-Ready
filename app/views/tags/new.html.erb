<form action="<%= tags_url %>" method="post">
  <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
  <label for="tag_name">
  <input type="text" name="tag[name]" value="<%= @tag.name %>" id="tag_name">
  <input type="Submit" value="Add Tag">
</form>
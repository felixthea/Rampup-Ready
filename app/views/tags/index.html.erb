<div class="tags-wrapper">
  <h2>Tags</h2>
  <ul>
    <% @tags.each do |tag| %>
      <li>
        <%= tag.name %>
        <%= button_to "Delete", tag_url(tag.id), method: :delete %>
      </li>
    <% end %>
  </ul>
  <%= link_to "Create Tag", new_tag_url %>
</div>
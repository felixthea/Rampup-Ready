<div class="subdivisions-wrapper">
  <h2>Subdivisions</h2>
  <ul>
    <% @subdivisions.each do |subdivision| %>
      <li>
        <%= link_to subdivision.name, subdivision_url(subdivision.id) %>
      </li>
    <% end %>
  </ul>
  <%= link_to "Add Subdivision", new_subdivision_url %>
</div>
<div class="sent-messages-wrapper">
  <h2>Sent Messages</h2>
  <ul>
    <% @messages.each do |message| %>
      <li>
        Subject: <%= link_to message.subject, message_url(message.id) %> | sent to <%= User.find(message.recipient_id).email %>
      </li>
    <% end %>
  </ul>
</div>